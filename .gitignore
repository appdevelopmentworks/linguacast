# Node / Next.js
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Rust / Tauri
src-tauri/target/
src-tauri/gen/schemas/
# Bundled sidecar binary (built by scripts/build-sidecar.py; rebuilt in CI)
src-tauri/binaries/
sidecar/build/
sidecar/dist/
sidecar/*.spec

# Python sidecar
sidecar/.venv/
sidecar/**/__pycache__/
sidecar/.ruff_cache/
*.pyc

# Dev-mode data root (LINGUACAST_DATA_DIR) — job artifacts, settings
.dev-data/
probe*.bat
probe-*.txt

# Secrets / env (never commit API keys — see CLAUDE.md guardrails)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
